<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= title %></title>
    <style>
        li{display:inline-block;}
    </style>
  </head>
  <body class="index">
    <main>
    <nav>
      <ul>
        <% if (user) { %>
        <li>
        </li>
        <% } else { %>
        <li>
         
          <a href="/auth/google">Login with Google</a>
        </li>
        <% } %>
      </ul>
    </nav>
<main>
  <h1><%if(user){%><img class="icon" src="<%=user.avatar%>"> <%}else{%> Welcome! <% } %>  </h1>
</main>

<%if(user){%>
  <section class="options">
    
    <a id="bubble" href= "/projects/<%=user._id%>">My Projects</a>
    <br>
    <br>
    <a id="bubble" href= "/projects/task/user/<%=user._id%>">My Tasks</a>
    <br>
    <br>
    <a id="bubble" href = "/projects/task">All Tasks </a>
    <br>
    <br>
    <a id="bubble" href = "/projects">All Projects</a>
    <br>
    <br>
    <a id="bubble" href="/auth/logout">Log Out</a>
  </section>
  
  <%}%>
</main>
<%- include('partials/footer') %>
